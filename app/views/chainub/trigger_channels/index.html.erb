<h1>Listing channels</h1>

<table>
  <thead>
    <tr>
      <th>Api key</th>
      <th>Api secret</th>
      <th>Name</th>
      <th>Description</th>
      <th>Url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @channels.each do |channel| %>
      <tr>
        <td><%= channel.api_key %></td>
        <td><%= channel.api_secret %></td>
        <td><%= channel.name %></td>
        <td><%= channel.description %></td>
        <td><%= channel.url %></td>
        <td><%= link_to 'Show', channel %></td>
        <td><%= link_to 'Edit', edit_trigger_channel_path(channel) %></td>
        <td><%= link_to 'Destroy', channel, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Channel', new_trigger_channel_path %>
